{
  "name": "downloadx-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "fmt": "oxfmt",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.54.0",
    "typescript": "^5.6.0"
  }
}
